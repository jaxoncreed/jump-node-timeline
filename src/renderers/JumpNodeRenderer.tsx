import { JumpNode } from "../entities/entityTypes";
import EntityRenderer from "./EntityRenderer";

const JumpNodeRenderer: EntityRenderer<JumpNode> = () => {
  return null;
};

export default JumpNodeRenderer;
